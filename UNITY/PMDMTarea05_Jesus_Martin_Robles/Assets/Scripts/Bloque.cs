using UnityEngine;

// Clase Bloque que representa un bloque en el juego de corredor infinito
public class Bloque : MonoBehaviour
{
    public int Tamaño; // Tamaño del bloque
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
